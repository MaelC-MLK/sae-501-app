import { Inter, K2D } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], display: 'swap' });
const k2d = K2D({ weight: "400", subsets: ['latin'], display: 'swap' });

export { inter, k2d };